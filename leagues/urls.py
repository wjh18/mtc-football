from django.urls import path
from . import views

urlpatterns = [
    path('', views.LeagueListView.as_view(), name='league_list'),
    path('<uuid:pk>/', views.LeagueDetailView.as_view(), name='league_detail'),
    path('new/', views.LeagueCreateView.as_view(), name='league_create'),
    path('<uuid:pk>/edit/', views.LeagueUpdateView.as_view(), name='league_edit'),
    path('<uuid:pk>/delete/', views.LeagueDeleteView.as_view(), name='league_delete'),
    path('<uuid:league>/teams/', views.TeamListView.as_view(), name='team_list'),
    path('<uuid:league>/teams/update-user-team/',
         views.update_user_team, name='update_user_team'),
    path('<uuid:league>/teams/<uuid:pk>/',
         views.TeamDetailView.as_view(), name='team_detail'),
    path('<uuid:league>/teams/<uuid:team>/roster/',
         views.TeamRosterView.as_view(), name='team_roster'),
    path('<uuid:league>/teams/<uuid:team>/depth-chart/',
         views.DepthChartView.as_view(), name='depth_chart'),
    path('<uuid:league>/teams/<uuid:team>/depth-chart/<str:position>/',
         views.DepthChartView.as_view(), name='depth_chart_pos'),
    path('<uuid:league>/teams/<uuid:team>/roster/<uuid:pk>/',
         views.PlayerDetailView.as_view(), name='player_detail'),
    path('<uuid:league>/advance-regular-season/<int:weeks>/',
         views.advance_regular_season, name='advance_regular_season'),
    path('<uuid:league>/advance-regular-season/',
         views.advance_regular_season, name='advance_regular_season_full'),
    path('<uuid:league>/standings/',
         views.LeagueStandingsView.as_view(), name='league_standings'),
    path('<uuid:league>/standings/<str:type>/',
         views.LeagueStandingsView.as_view(), name='league_standings_type'),
    path('<uuid:league>/teams/<uuid:team>/schedule/',
         views.TeamScheduleView.as_view(), name='team_schedule'),
    path('<uuid:league>/weekly-matchups/',
         views.WeeklyMatchupsView.as_view(), name='weekly_matchups'),
    path('<uuid:league>/weekly-matchups/<int:week_num>/',
         views.WeeklyMatchupsView.as_view(), name='weekly_matchups_by_week'),
    path('<uuid:league>/weekly-matchups/matchup/<int:pk>/',
         views.MatchupDetailView.as_view(), name='matchup_detail'),
]
